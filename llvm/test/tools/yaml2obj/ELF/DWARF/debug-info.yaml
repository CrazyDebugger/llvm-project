## Test that yaml2obj emits .debug_info section.

## a) TODO: Generate the .debug_info section from the "DWARF" entry.
# RUN: not yaml2obj --docnum=1 %s -o %t1.o 2>&1 | FileCheck %s --check-prefix="NOIMPL"

# NOIMPL: yaml2obj: error: .debug_info section is not implemented

--- !ELF
FileHeader:
  Class:   ELFCLASS64
  Data:    ELFDATA2LSB
  Type:    ET_EXEC
  Machine: EM_X86_64
DWARF:
  debug_info:
    - Length:
        TotalLength: 0x0
      Version:       0
      AbbrOffset:    0x0
      AddrSize:      0x0
      Entries:
...
